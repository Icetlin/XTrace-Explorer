[www]
clear_env = no
; Override the base image's default listen = 9000 (zz-docker.conf). Port 9000
; on the host is commonly taken by PhpStorm/Xdebug, and this container runs in
; network_mode: host, so we move php-fpm to 9010 to avoid the collision.
listen = 127.0.0.1:9010
